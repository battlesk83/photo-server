# TTS Manager · Voice 확인 가이드

## angel / fact 호출 예시

```dart
import 'package:seongyu_samchon_counsel/services/tts_manager.dart';

// 싱글톤 사용 (앱 전역 동일 인스턴스)
final tts = TtsManager();

// 🌤 천사썬: 젊고 부드러운 남자 (pitch 0.97, rate 0.52)
await tts.speakAngel(
  '괜찮아요. 지금까지 정말 잘 버텨오셨어요. 잠시 숨을 고르고, 천천히 하나씩 풀어가봅시다.',
);

// 🔥 팩폭썬: 카리스마 남자 (pitch 0.88, rate 0.58)
await tts.speakFact(
  '야야. 현실 좀 보자. 지금 상황에서 감정만 붙잡고 있으면 아무것도 안 바뀐다. 움직여야 된다.',
);

// 재생 중단
await tts.stop();
```

## 에뮬/실폰에서 voice 목록 확인 방법

1. **앱 실행 후 디버그 로그**
   - `flutter run` 후 TTS가 처음 사용될 때(예: 메인 화면에서 "위로의 말" 또는 "팩폭 한마디" 탭) 콘솔에 다음이 출력됩니다.
   - `[TtsManager] getEngines: [...]`  
   - `[TtsManager] getVoices: [...]`  
   - `[TtsManager] selected angel voice: {name: ..., locale: ko-KR}`  
   - `[TtsManager] selected fact voice: {...}`  
   - `[TtsManager] current voice: {...}` (speak 시)

2. **Android 에뮬레이터**
   - 설정 → 접근성 → 텍스트 음성 변환 출력 → 엔진/언어에서 "Google 텍스트 음성 변환" 및 "한국어" 선택 시 사용 가능한 음성이 기기마다 정해집니다.
   - 앱에서 위 로그로 실제 선택된 `name`/`locale`을 확인할 수 있습니다.

3. **실기기**
   - 동일하게 `flutter run` 후 로그에서 `getVoices`·`selected angel/fact voice`·`current voice`를 확인하면, 해당 기기에서 적용된 voice를 알 수 있습니다.

4. **요약**
   - **에뮬**: `flutter run` → TTS 한 번 호출 → 콘솔에서 `[TtsManager] getVoices` / `selected ... voice` 확인.  
   - **실폰**: USB 디버깅으로 `flutter run` 후 동일하게 콘솔 로그 확인.
